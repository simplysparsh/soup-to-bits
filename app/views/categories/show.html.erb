<h1><%= @category.name %></h1>

<em>The soups available in this category are:</em>

<ul>
  <% @category.soups.each do |soup| %>
  <li>
    <%= link_to soup.name, soup %>
  </li>
  <% end %>
</ul>

<p>
  <%= link_to "Back to Soup categories", categories_path %>
</p>
